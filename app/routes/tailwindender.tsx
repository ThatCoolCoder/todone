export default function TailwindEnder() {
    // basically a bunch of classnames forcing tailwind to not ignore stuff
    // named like this because this makes me want to end tailwinds existence

    const names = [
    "bg-red-500",
    "bg-stone-900",
    "bg-zinc-800",
];
}